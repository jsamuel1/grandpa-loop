{
  "name": "default",
  "resources": [
    "file://README.md",
    "skill://.kiro/skills/**/SKILL.md",
    {
      "include": ["**/*.md"],
      "indexType": "best",
      "autoUpdate": true,
      "name": "project-notes",
      "description": "Project notes and documentation",
      "source": "file://.",
      "type": "knowledgeBase"
    }
  ],
  "description": "General coding tasks, file operations, and web research. Default agent for the Ralph orchestration loop.",
  "toolsSettings": {
    "execute_bash": {
      "autoAllowReadonly": true
    },
    "write": { "allowedPaths": ["./**"] },
    "fs_write": { "allowedPaths": ["./**"] },
    "subagent": {
      "availableAgents": ["*"],
      "trustedAgents": ["default", "visual", "kiro_default", "git"]
    }
  },
  "prompt": "Use subagents to run independent tasks in parallel to reduce wall-clock time. Use subagents for investigations (e.g., analyzing debug/log output) to optimize context window usage. Always determine today's date before working with dates.",
  "tools": ["*"]
}
